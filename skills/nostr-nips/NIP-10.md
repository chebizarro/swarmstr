# NIP-10 Reference Card

### REFERENCE_CARD
NIP-10 defines `kind:1` for simple plaintext notes. Required tags: none. Optional tags: `e` (for note threads and replies), `q` (for citing events), `p` (for involved pubkeys). Filter patterns: `kind:1`, `e` tags for reply threads. Common use cases: posting notes, replying to notes, citing events. 
```json
{
  "kind": 1,
  "content": "Some text",
  "tags": [
    ["e", "<event-id>", "<relay-url>", "<marker>", "<pubkey>"],
    ["q", "<event-id> or <event-address>", "<relay-url>", "<pubkey-if-a-regular-event>"],
    ["p", "<pubkey>"]
  ]
}
```

### PINNED_FACTS
```json
[
  "kind:1 = simple plaintext note, no required tags",
  "e tags are used for note threads and replies, with optional markers 'reply' and 'root'",
  "p tags record involved pubkeys in a reply thread"
]
```

---
*Generated by NIP Reference Pipeline*

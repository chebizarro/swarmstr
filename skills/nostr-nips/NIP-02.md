# NIP-02 Reference Card

### REFERENCE_CARD
NIP-02 defines a "follow list" event with kind `3`. It contains a list of `p` tags, each with a profile key, relay URL, and local name (petname). The `.content` field is not used. 

Example:
```json
{
  "kind": 3,
  "tags": [
    ["p", "91cf9..4e5ca", "wss://alicerelay.com/", "alice"],
    ["p", "14aeb..8dad4", "wss://bobrelay.com/nostr", "bob"],
    ["p", "612ae..e610f", "ws://carolrelay.com/ws", "carol"]
  ],
  "content": ""
}
```
Required tags: `kind` (3), `tags` (list of `p` tags).
Optional tags: `content` (not used).
Filter patterns: `kind = 3`.
Common use cases: follow list backup, profile discovery, relay sharing, petname scheme.

### PINNED_FACTS
```json
[
  "kind:3 = follow list event with p tags for each followed profile",
  "each p tag contains: profile key, relay URL, and local name (petname)",
  "new follow lists overwrite past ones, so they should contain all entries"
]
```

---
*Generated by NIP Reference Pipeline*

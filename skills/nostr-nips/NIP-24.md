# NIP-24 Reference Card

### REFERENCE_CARD
NIP-24 defines extra metadata fields and tags for Nostr events. 
- kind 0: Optional fields for metadata events include `display_name`, `website`, `banner`, `bot`, and `birthday`. 
- kind 3: No new fields are defined, but deprecated fields include `{<relay-url>: {"read": <true|false>, "write": <true|false>}, ...}`.
- Tags: `r` (web URL), `i` (external id), `title` (name of sets, calendar events, live events, or listings), and `t` (hashtag, lowercase string).
Example JSON for kind 0: `{"name": "John", "display_name": "John Doe", "website": "https://johndoe.com"}`.
Common use cases: Adding extra information to user profiles, specifying automation sources, and referencing external resources.

### PINNED_FACTS
["kind:0 = metadata events with optional fields like display_name and website", "kind:3 = follow events with no new fields, but deprecated relay objects", "tags like r, i, title, and t can be used across multiple event kinds"]

---
*Generated by NIP Reference Pipeline*

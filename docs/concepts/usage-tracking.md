---
summary: "Token usage tracking and provider quota monitoring in swarmstr"
read_when:
  - Monitoring API usage and costs
  - Understanding token tracking implementation
title: "Usage Tracking"
---

# Usage Tracking

swarmstr tracks token usage per turn and surfaces usage information via the status command and session transcripts.

## What's Tracked

Per agent turn:
- **Input tokens**: tokens in the prompt (context + user message + tools)
- **Output tokens**: tokens generated by the model
- **Cache read tokens**: tokens read from Anthropic's prompt cache (cheaper)
- **Cache write tokens**: tokens written to cache

Accumulated per session:
- Total token count
- Token count by role (user, assistant, tool)
- Cumulative cost estimate (based on provider pricing)

## Viewing Usage

```bash
# Current status
swarmstr status
swarmstr status --json

# List models (shows which are configured)
swarmstr models list
```

## Usage in Transcripts

Token counts are stored in session transcript events on Nostr. The `meta` field of each `TranscriptEntryDoc` carries token usage for that turn.

## Provider Usage APIs

Token usage is tracked in session transcript metadata. Per-provider quota information is not currently surfaced via the CLI. Check your provider dashboard for quota and spend information.

## Usage Alerts

Swarmstr does not currently have built-in token/cost alert thresholds. Monitor usage via `swarmstr status` or `swarmstr models list`, or build alerting around the webhook integration.

## See Also

- [Token Use & Prompt Caching](/reference/token-use)
- [Authentication](/gateway/authentication)
- [Auth Monitoring](/automation/auth-monitoring)
